name: produtos
description: "a package owner by products squad to provide logics to both facilita apps (broker and manager)."
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  core:
    path: ../core

  design:
    path: ../design

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
